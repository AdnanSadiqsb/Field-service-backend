{
  "installCommand": "pip install --upgrade pip setuptools wheel && pip install -r requirements/vercel.txt",
  "buildCommand": "echo 'Skipping collectstatic - run manually with S3 credentials'",
  "env": {
    "DJANGO_SETTINGS_MODULE": "src.config.production",
    "PYTHONUNBUFFERED": "1",
    "PYTHONPATH": "."
  },
  "regions": ["iad1"],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
